{
  "name": "prima789-line-member",
  "version": "1.0.0",
  "description": "Prima789 LINE Member Card System",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step required'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "netlify-cli": "^23.5.1"
  }
}
